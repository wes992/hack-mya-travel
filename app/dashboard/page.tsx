import { Grid } from "@mui/material";
import React from "react";

const Dashboard = () => {
  return (
    <Grid container mt={2} p={2} borderRadius={2} gap={2}>
      {/* //TODO: Remove bgcolor */}
      <div className="">Dashboard</div>
    </Grid>
  );
};

export default Dashboard;
